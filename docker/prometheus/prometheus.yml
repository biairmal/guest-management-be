global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Uncomment once the app exposes a /metrics endpoint (go-sdk metrics package, planned):
  # - job_name: guest-management-be
  #   static_configs:
  #     - targets: ["host.docker.internal:8080"]
